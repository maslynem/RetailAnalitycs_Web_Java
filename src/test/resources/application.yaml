spring:
  liquibase:
    contexts: test
  datasource:
    username: test
    password: test

  jpa:
    properties:
      hibernate:
        hbm2ddl:
          auto: validate
        format_sql: true
        show_sql: true